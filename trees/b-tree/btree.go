package main

/*
B-tree of order m where m is maximum number of children per node

- Every node has <=m children
- Every node has >= m/2 children  (except root and leaves)
- All leaves are at the same level and have no info
- all non-leaf node with k children have k-1 keys


*/

func main() {

}
